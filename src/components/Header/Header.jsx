import React from 'react';
import { withRouter } from 'react-router-dom';

function SearchAppBar({ history }) {

  return (
        <p class="card-header">Nous contacter</p>
  );
}

export default withRouter(SearchAppBar);
